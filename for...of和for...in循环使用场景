for...of和for...in循环都可以用来遍历对象，不同点在于:

1、for...of遍历的是对象的值(value)，for...in遍历的是对象的键(key)
2、for...in循环能遍历任何类型的对象，for...of只能遍历部署了遍历器(Iterator)的对象（如：数组，map，set等）

此外，for...in循环有几个缺点。
  a、数组的键名是数字，但是for...in循环是以字符串作为键名“0”、“1”、“2”等等。
  b、for...in循环不仅遍历数字键名，还会遍历手动添加的其他键，甚至包括原型链上的键。
  c、某些情况下，for...in循环会以任意顺序遍历键名。
总之，for...in循环主要是为遍历对象而设计的，不适用于遍历数组。

对比for...in循环，for...of有着同for...in一样的简洁语法，但是没有for...in那些缺点。for...of提供了遍历所有数据结构的统一操作接口。

所以，遍历对象时如果对象部署了遍历器推荐使用for...of循环，对没有部署遍历器接口的对象才使用for...in循环。
